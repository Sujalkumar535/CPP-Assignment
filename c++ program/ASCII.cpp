#include<iostream>
using namespace std;
int main()
{
    char ch;
    int ascii;
    cout<<"enter the character";
    cin >>ch;
    ascii=int(ch);
    cout<<"the ascii value is "<<ascii;
    return 0;

}